- page_title "Manifest file import"
- header_title "项目", root_path

%h3.page-title
  = _('Manifest file import')

- if @errors.present?
  .alert.alert-danger
    - @errors.each do |error|
      = error

= render 'form'
